# Amvera Cloud — production app (Next.js), not the old nginx→Vercel proxy.
meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: amvera/Dockerfile

run:
  containerPort: 3000
  # Default /data mount is fine; app does not require persistent local files.
  persistenceMount: /data
