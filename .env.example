# Google Gemini API key for panel photo analysis
# Create key: https://aistudio.google.com/apikey
# Add the same variable in Vercel → Project → Settings → Environment Variables
#
# Optional: force a model (default tries gemini-2.5-flash, then lite fallbacks)
# GEMINI_MODEL=gemini-2.5-flash
GEMINI_API_KEY=
